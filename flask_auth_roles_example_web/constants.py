# URL base de la API del backend (flask-auth-roles-example-api)
API_BASE_URL = 'http://localhost:5000/flask_auth_roles_example_api'

# Roles soportados
ROL_ADMIN = 'admin'

# Reglas de dominio sobre el password (espejadas con la API)
PASSWORD_MIN_LEN = 6
PASSWORD_MAX_LEN = 64

# Timeout para las requests al backend (segundos)
REQUEST_TIMEOUT = 10
